# Telegram Бот для автоматизации подписки на канал

Этот Telegram бот автоматически проверяет подписку пользователей на ваш канал и выдает подарок (PDF файл) после успешной подписки.

## 📋 Что умеет этот бот?

- ✅ Проверяет подписку пользователей на ваш Telegram канал
- 🎁 Выдает PDF файл в качестве подарка за подписку
- 📊 Имеет команды для связи, консультаций и информации
- 🔄 Автоматически перезапускается при ошибках
- 🐳 Работает в Docker (простая установка)

## 🛠 Предварительные требования

Перед установкой убедитесь, что у вас установлено:

### Для Windows:
1. **Docker Desktop** - [Скачать здесь](https://www.docker.com/products/docker-desktop)
2. После установки запустите Docker Desktop и дождитесь надписи "Docker is running"

### Для Mac OS:
1. **Docker Desktop** - [Скачать здесь](https://www.docker.com/products/docker-desktop)
2. После установки запустите Docker из папки Applications

## 📝 Шаг 1: Подготовка бота и канала

### 1.1 Создайте Telegram бота
1. Найдите в Telegram `@BotFather`
2. Отправьте команду `/newbot`
3. Придумайте имя бота (например, "My Gift Bot")
4. Придумайте username бота (должен заканчиваться на `bot`, например `my_gift_bot`)
5. Сохраните токен бота (выглядит как `1234567890:ABCdefGHIjklMNopQRstUVwxyz`)

### 1.2 Создайте Telegram канал
1. В Telegram нажмите "Создать канал"
2. Придумайте название канала
3. Сделайте канал публичным
4. Запомните username канала (например, `@my_channel`)

### 1.3 Добавьте бота в канал
1. Зайдите в настройки канала
2. Выберите "Администраторы"
3. Добавьте бота как администратора
4. Дайте боту права:
   - ✅ "Изменять сообщения"
   - ✅ "Просматривать участников"

### 1.4 Получите ID канала
1. Добавьте бота `@userinfobot` в свой канал
2. Перешлите любое сообщение из канала боту `@userinfobot`
3. Он покажет ID канала (выглядит как `-1001234567890`)
4. Сохраните этот ID

## 📁 Шаг 2: Установка файлов бота

### 2.1 Скачайте файлы бота
Создайте папку для бота на вашем компьютере, например `telegram-bot`, и поместите в нее все файлы из репозитория.

### 2.2 Структура папок должна быть такой:
```
telegram-bot/
├── .env
├── docker-compose.yml
├── Dockerfile
├── index.js
├── package.json
└── storage/
    └── file.pdf (ваш PDF файл с подарком)
```

## ⚙️ Шаг 3: Настройка конфигурации

### 3.1 Создайте файл `.env`
Откройте блокнот (Windows) или TextEdit (Mac) и создайте файл с именем `.env` со следующим содержанием:

```env
# Токен вашего бота из @BotFather
BOT_TOKEN=8402137003:AAEVjFR22F1EcR0Nlt4gBW-bHjlfojUjrvs

# Username вашего канала (например: @my_channel)
CHANNEL_USERNAME=@your_channel_username

# ID вашего канала (например: -1001234567890)
CHANNEL_CHAT_ID=-1002172300055

# Ссылка на канал (например: https://t.me/my_channel)
CHANNEL_LINK=https://t.me/your_channel

# Ваш email для связи
EMAIL=your.email@example.com

# Ссылка для консультаций
CONSULTATION_LINK=https://calendly.com/your-link

# Ссылка на интенсив
INTENSIVE=https://your-intensive-link.com
```

**Замените все значения на свои!**

### 3.2 Подготовьте PDF файл
Поместите ваш PDF файл с подарком в папку `storage` и назовите его `toxik.pdf`

## 🚀 Шаг 4: Запуск бота

### Для Windows:

1. **Откройте командную строку:**
   - Нажмите `Win + R`
   - Введите `cmd`
   - Нажмите `Enter`

2. **Перейдите в папку с ботом:**
   ```cmd
   cd C:\путь\к\папке\telegram-bot
   ```

3. **Запустите бота:**
   ```cmd
   docker-compose up -d
   ```

### Для Mac OS:

1. **Откройте Terminal:**
   - Нажмите `Cmd + Space`
   - Введите `Terminal`
   - Нажмите `Enter`

2. **Перейдите в папку с ботом:**
   ```bash
   cd /путь/к/папке/telegram-bot
   ```

3. **Запустите бота:**
   ```bash
   docker-compose up -d
   ```

## ✅ Шаг 5: Проверка работы

После запуска:

1. **Проверьте статус бота:**
   ```bash
   docker-compose logs -f
   ```
   Должны увидеть: `✅ Бот успешно запущен в Docker контейнере`

2. **Найдите вашего бота в Telegram** по username
3. **Начните диалог** командой `/start`
4. **Протестируйте функционал** - бот должен предложить подписаться на канал

## 🎯 Доступные команды бота

- `/start` - начать работу с ботом
- `/getagift` - получить подарок за подписку
- `/towrite` - получить email для связи
- `/getaconsultation` - записаться на консультацию
- `/intensive` - получить ссылку на интенсив
- `/importantinformation` - важная информация

## 🛠 Управление ботом

### Остановка бота:
```bash
docker-compose down
```

### Перезапуск бота:
```bash
docker-compose restart
```

### Просмотр логов:
```bash
docker-compose logs -f
```

### Обновление бота (после изменений в коде):
```bash
docker-compose down
docker-compose up -d --build
```

## ❌ Решение возможных проблем

### Проблема: "Docker is not running"
**Решение:** Запустите Docker Desktop и дождитесь полной загрузки

### Проблема: "Cannot connect to Docker daemon"
**Решение:** Закройте и снова откройте командную строку/Terminal

### Проблема: Бот не отвечает
**Решение:** Проверьте логи командой `docker-compose logs -f`

### Проблема: "Chat not found"
**Решение:** Проверьте правильность CHANNEL_CHAT_ID в файле `.env`

### Проблема: "Forbidden: bot is not a member"
**Решение:** Добавьте бота как администратора в канал

## 📞 Поддержка

Если у вас возникли проблемы с установкой или работой бота:

1. Проверьте, что все шаги установки выполнены правильно
2. Убедитесь, что все переменные в `.env` файле заполнены корректно
3. Проверьте логи бота командой `docker-compose logs -f`
4. Убедитесь, что бот добавлен в канал как администратор

## 🔄 Обновление бота

Для обновления бота до новой версии:

1. Остановите текущую версию: `docker-compose down`
2. Скачайте новые файлы бота
3. Запустите снова: `docker-compose up -d`

---

**Ваш бот готов к работе!** 🎉 Теперь он будет автоматически проверять подписку пользователей и выдавать подарки за подписку на ваш канал.